#include "main.h"
#include "string.h"

/**
 * *_strcpy - function
 * @dest: pointer varuable
 * @src: pointer variable
 * Return: dest
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);

	return (dest);
}
